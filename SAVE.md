# 캐릭터별 저장 — v0.10.0

## 스키마

`file-island-echoes:v3`에 `{version:3,runs,activeCharacterId,archive,settings}`를 한 번의 setItem으로 저장한다. `runs`는 알려진 캐릭터 8개 키의 `{run,updatedAt}` 슬롯이다. 키와 run.characterId가 일치해야 하며 활성 ID는 실제 슬롯만 가리킨다. 별도 단일 run 복사본을 저장하지 않는다. Archive.runs는 누적 완료 횟수다.

`activeRun`, `slotFor`, `saveRun`, `activateSlot`으로 접근한다. 엔진은 계속 Run만 처리한다. 성공한 진행 변경 때만 해당 슬롯의 updatedAt을 갱신한다. 홈 이동·슬롯 전환·설정 변경은 시각과 손패·난수를 바꾸지 않는다. 결과를 다시 열어도 통계를 추가하지 않는다.

## 이관과 손상

로드 우선순위는 v3 → v2 → v1. v2는 기존 Run을 해당 캐릭터에, v1은 기존 안전 변환 뒤 신태일에 넣는다. 첫 v3 기록은 기존 시작 시각을 슬롯 시각으로 사용한다. v3 쓰기가 실패해도 구키는 남는다. 정상 v3가 있으면 비어 있어도 구키로 돌아가지 않는다. 구키는 전체 초기화 때만 제거한다.

손상 슬롯은 그 캐릭터 이름과 함께 제외하고 다른 슬롯·공용 기록은 보존한다. 잘못된 active ID는 최근 정상 슬롯으로 바꾼다. 미지 키는 제외하며 중복 JSON 키(이스케이프 포함), prototype 관련 키는 파싱 전에 거부한다. 전체 JSON이 파손되어 읽을 수 없는 경우에는 빈 프로필과 경고를 표시한다.

## 저장 실패와 여러 탭

SaveSession은 마지막 성공한 프로필을 메모리에 보관한다. 실패하면 Run·활성 ID·공용 기록·설정을 모두 되돌린다. 모션 구간에서는 홈/슬롯 전환을 잠시 막는다. 브라우저가 저장을 차단하면 진행 변경을 확정하지 않으며 오류를 표시한다.

storage 이벤트와 마지막으로 읽은 원문 비교로 외부 변경을 감지한다. 오래된 탭은 진행을 쓰지 않고 최신 기록 불러오기를 제공한다. 자동 병합은 하지 않는다. localStorage에는 교차 탭 compare-and-swap이 없으므로 여러 탭에서 정확히 동시에 일어난 read/write를 분산 트랜잭션처럼 보장하지는 않는다. 한 탭에서 플레이하는 것을 권한다. 구버전 탭은 업데이트한 뒤 사용한다.

## 시작과 전체 백업

다른 아이는 별도 슬롯으로 시작한다. 같은 아이의 챕터 출발 때만 캐릭터·현재 챕터·위치를 표시하고 해당 슬롯 교체를 확인한다. 도감과 설정은 공유한다. 타이틀의 대표 버튼은 최근 진행을 이름과 함께 표시하며 다른 탐험 선택에서 모든 슬롯을 연다.

JSON formatVersion 2는 8슬롯 전체와 활성 ID·저장 시각·공용 기록·설정을 담는다. formatVersion 1 및 v1/v2 원본도 가져온다. 불러오기는 미리보기 후 전체 교체이며 병합하지 않는다. 단일 setItem 성공 뒤 화면을 바꾼다. 모든 기록 초기화는 v1/v2/v3 키만 삭제한다.

입력 제한: UTF-8 8MiB, 깊이 16, 노드 100,000, 배열 999개, 문자열 2,048자, 필드명 64자, 유한 숫자 절댓값 10^15. 캐릭터 슬롯은 8개. `scripts/measure-slot-save.mjs`로 현재 카드/적 문구와 큰 숫자를 포함한 최장 로그를 계산했다. 로그 144B × 999행 × 8슬롯의 들여쓰기 포함 백업은 1,315,123B로 이전 1MiB를 넘는다. 8MiB 제한은 이를 수용하며 임의로 부풀린 입력은 계속 거부한다. 실제 localStorage quota는 브라우저마다 다르다.

PWA HTTP 캐시와 게임 저장은 분리되어 있다. 업데이트 전 전체 프로필을 저장하며 음악 캐시 복구는 슬롯을 건드리지 않는다. 중요한 기록은 JSON 백업을 권한다.
