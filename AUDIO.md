# File Island Echoes · 오디오 제작 및 사용 기록

2026-09-10 · v0.5. 외부 음악·효과음·보이스 샘플을 사용하지 않았습니다. 특정 공식 곡, 작곡가 또는 성우를 참조하거나 모방하도록 요청하지 않았습니다. 모든 선율·음색·타악기는 이 프로젝트를 위해 코드로 새로 작성했습니다.

## 제작

전용 음악 생성 도구가 제공되지 않아 Python/NumPy/SciPy로 악보와 신시사이저를 작성했습니다. `scripts/compose-audio.py`에 실제 멜로디, 화성, 템포, 악기와 믹스가 들어 있습니다. 감쇠 배음으로 뜯는 현 음색, 목재 타악기, 숨이 섞인 관악기, 패드, 종, 베이스와 드럼을 합성합니다. 8마디 질문/대답 선율과 대조 구간을 배치하고 지역별 악기·리듬을 달리했습니다. 단일 테스트 비프음이나 공식 음원 추출물이 아닙니다.

32kHz 스테레오 마스터를 생성한 뒤 FFmpeg의 libvorbis(q=1.5), libmp3lame(64kbps)로 인코딩했습니다. OGG를 우선 지원하는 브라우저에서는 OGG, 나머지는 MP3를 사용하며 실패하면 대체 형식을 시도합니다. WAV 마스터와 검수용 메들리는 `test-results/`에만 생성되며 Git과 배포에서 제외합니다.

음악은 약 -20dBFS RMS를 목표로 조절하고, 효과음은 약 -18dBFS RMS를 목표로 하되 피크를 우선 제한했습니다. 실제 압축 결과는 `docs/audio-measurements.json`에 있습니다. 음악에 순환 잔향을 적용해 끝의 잔향이 다음 시작으로 이어집니다. 반복 음악에는 일반적인 곡 전체 페이드인/아웃이나 패딩을 넣지 않습니다. 압축 디코딩이 남기는 경계 오프셋은 재생 전 양 끝 2ms의 코사인 보정으로 제거해 첫·마지막 샘플을 연결합니다. 길이·박자·중앙 파형은 유지합니다.

## 음악 배치

| ID | 곡 이름 | 사용 화면 | 길이 |
| --- | --- | --- | --- |
| `music-title` | 물결 너머의 첫 신호 | 타이틀, 첫 입력 이후 | 65.5초 |
| `music-selection` | 여덟 갈래의 작은 빛 | 캐릭터 선택·상세·도감 | 72.0초 |
| `music-map` | 발자국을 잇는 지도 | 챕터 선택·첫 지도·서버대륙 외곽·도시 초입 | 70.0초 |
| `music-forest` | 잎 사이로 흐르는 데이터 | 파일섬 숲 지도·휴식·보상 후 | 64.0초 |
| `music-factory` | 멈춘 기계의 작은 맥박 | 공장·통신소 지도·휴식·보상 후 | 68.6초 |
| `music-mountain` | 바람이 남긴 좌표 | 무한산·도시 후반 지도·휴식·보상 후 | 70.2초 |
| `music-battle` | 서로의 다음 한 수 | 일반 전투 | 66.2초 |
| `music-elite` | 흔들리지 않는 발걸음 | 강적 전투 | 64.0초 |
| `music-boss` | 어둠 너머로 건네는 신호 | 데블몬 | 64.0초 |
| `music-boss-etemon` | 뒤틀린 방송을 넘어 | 에테몬 · 금속 타악기 변주 | 64.0초 |
| `music-boss-myotismon` | 새벽을 기다리는 신호 | 묘티스몬 · 관악기와 바람 변주 | 64.0초 |
| `music-story` | 말하지 못했던 마음 | 도입·이야기 이벤트·결과 징글 이후 | 75.8초 |

## 징글과 효과음

진화 시작/강제 진화는 각각 6.5초입니다. 진화 장면에서는 음악을 내리고 데이터 집중과 진화 스팅어를 재생합니다. 다음으로 진행하면 남은 연출은 짧게 페이드하고 완료·카드 강화 소리와 지역 음악으로 이어집니다. 장면 전환은 오디오 종료를 기다리지 않습니다. 문장 소리는 8명마다 다릅니다.

| ID | 용도 | 종류 | 길이 |
| --- | --- | --- | --- |
| `crest-ready` | 문장 준비 완료 | stinger | 1.50초 |
| `evolution-start` | 새로운 형태로 모이는 마음 | stinger | 6.50초 |
| `evolution-complete` | 함께 닿은 새 모습 | stinger | 2.50초 |
| `evolution-forced` | 균열 속의 힘 | stinger | 6.50초 |
| `battle-victory` | 열린 길 | stinger | 2.80초 |
| `battle-defeat` | 다시 손을 잡고 | stinger | 3.20초 |
| `card-acquire` | 기억한 기술 | stinger | 1.30초 |
| `chapter-complete` | 서로에게 돌아오는 목소리 | stinger | 5.00초 |
| `archive-unlock` | 새로운 기록 | stinger | 1.60초 |
| `crest-tai` | 용기 · 금속의 도약 | stinger | 1.90초 |
| `crest-matt` | 우정 · 맞닿은 두 음색 | stinger | 2.00초 |
| `crest-sora` | 사랑 · 따뜻한 공명 | stinger | 2.20초 |
| `crest-koushiro` | 지식 · 정돈된 신호 | stinger | 1.60초 |
| `crest-mimi` | 순수 · 잎 끝의 반짝임 | stinger | 2.00초 |
| `crest-joe` | 성실 · 단단한 약속 | stinger | 1.80초 |
| `crest-tk` | 희망 · 작은 상승 | stinger | 2.30초 |
| `crest-kari` | 빛 · 맑아지는 울림 | stinger | 2.30초 |
| `ui-confirm` | 버튼 누르기 | sfx | 0.26초 |
| `ui-select` | 선택 변경 | sfx | 0.29초 |
| `ui-cancel` | 뒤로 가기 | sfx | 0.32초 |
| `ui-denied` | 불가능한 행동 | sfx | 0.39초 |
| `node-select` | 지도 이동 | sfx | 0.54초 |
| `reward-reveal` | 보상 펼치기 | sfx | 0.84초 |
| `card-draw` | 카드 드로우 | sfx | 0.28초 |
| `card-select` | 카드 선택 | sfx | 0.22초 |
| `card-attack` | 공격 카드 | sfx | 0.37초 |
| `card-guard` | 방어 카드 | sfx | 0.44초 |
| `card-support` | 지원 카드 | sfx | 0.54초 |
| `card-evolved` | 진화 기술 카드 | sfx | 0.74초 |
| `card-discard` | 카드 버림 | sfx | 0.32초 |
| `deck-shuffle` | 덱 섞기 | sfx | 0.64초 |
| `energy-low` | 에너지 부족 | sfx | 0.33초 |
| `card-upgrade` | 카드 강화 | sfx | 0.74초 |
| `hit-normal` | 일반 타격 | sfx | 0.29초 |
| `hit-heavy` | 강한 타격 | sfx | 0.54초 |
| `attack-fire` | 화염 | sfx | 0.74초 |
| `attack-electric` | 전기 | sfx | 0.59초 |
| `attack-water` | 물과 얼음 | sfx | 0.74초 |
| `attack-plant` | 식물 속박 | sfx | 0.69초 |
| `attack-light` | 빛 | sfx | 0.79초 |
| `attack-dark` | 어둠 | sfx | 0.79초 |
| `block-gain` | 방어도 획득 | sfx | 0.49초 |
| `block-break` | 방어 파괴 | sfx | 0.51초 |
| `heal` | 회복 | sfx | 0.84초 |
| `status-apply` | 상태 이상 | sfx | 0.59초 |
| `status-cleanse` | 상태 정화 | sfx | 0.79초 |
| `enemy-intent` | 행동 예고 | sfx | 0.39초 |
| `enemy-appear` | 적 등장 | sfx | 0.74초 |
| `player-hit` | 파트너 피격 | sfx | 0.39초 |
| `enemy-hit` | 적 피격 | sfx | 0.36초 |
| `enemy-purify` | 적 정화 | sfx | 1.19초 |
| `enemy-down` | 적 격퇴 | sfx | 0.79초 |
| `crest-charge` | 문장 에너지 | sfx | 0.44초 |
| `evolution-energy` | 진화 에너지 | sfx | 0.59초 |
| `data-gather` | 데이터 집중 | sfx | 0.99초 |
| `forced-warning` | 강제 진화 경고 | sfx | 1.29초 |
| `corruption-rise` | 오염 증가 | sfx | 0.79초 |
| `burden-relief` | 부담 감소 | sfx | 0.74초 |

## 파일 연결과 믹서

- 에셋: `public/assets/audio/{music,stingers,sfx}/`. 파일명은 위 ID에 `.ogg` 또는 `.mp3`입니다.
- 중앙 매니페스트: `src/audio-manifest.json`. 경로·음량·루프 길이·변형 폭·쿨다운·동시 제한·페이드 시간을 관리합니다.
- 카드와 문장 데이터의 `audioId`가 재생 ID를 참조합니다. 화면에 경로 문자열을 반복하지 않습니다.
- `src/audio.ts`: 하나의 AudioContext, Master/Music/SFX 버스, 징글 중 음악 덕킹, 최종 피크 압축기, 비동기 로드 및 포맷 대체.
- `src/audio-events.ts`: 화면 음악 결정과 읽기 전용 전후 상태 비교. 전투 규칙·난수·저장된 덱은 오디오 코드가 변경하지 않습니다.
- 기본 음량: 전체 80%, 음악 65%, 효과음 80%. 처음은 음소거입니다. 타이틀과 헤더에 소리 켜기, 설정에 슬라이더·미리 듣기·출력 계기가 있습니다.
- 첫 사용자 입력 전에는 AudioContext와 파일 요청을 만들지 않습니다. 파일은 해당 화면/효과가 필요할 때 요청합니다.
- 같은 음악은 다시 요청해도 재시작하지 않습니다. 다른 음악을 거쳐 돌아오면 보관한 위치에서 이어갑니다. 음악 사이 0.65초 크로스페이드, 진화/징글에서는 잠시 정지 또는 덕킹합니다.
- 짧은 효과음은 음높이와 음량을 약 ±1.75% 변형합니다. 동일 효과 최대 2개, 140ms 간격, 전체 음성 최대 12개로 제한하며 중요한 징글을 우선합니다.
- 백그라운드 visibilitychange/pagehide에서 컨텍스트를 멈춥니다. 복귀 시 재개하고, 브라우저가 거절하면 다음 사용자 입력에서 다시 시도합니다.
- 로드 실패는 해당 큐만 건너뜁니다. 두 코덱 모두 실패한 큐는 매 렌더마다 반복 요청하지 않습니다. 설정에 조용한 안내를 표시합니다.
- 디코딩한 음악은 최대 3곡을 캐시합니다. 곡당 약 16~19MiB이며 효과음은 소량의 별도 캐시를 재사용합니다. 모든 음악을 최초에 디코딩하지 않습니다.

## 용량과 저장

총 12곡 + 징글 17개 + 효과음 41개 = **70개 큐, 140개 압축 파일**입니다. 두 형식 합계 **14,490,553 bytes / 13.82MiB**입니다. 첫 입력 전 오디오 전송은 0, 타이틀 활성화 후에는 지원 형식 하나만 약 **0.50~0.54MiB** 요청합니다.

기존 10MiB 예산 검사는 비오디오 리소스에 유지하고, 이번 요청의 오디오 전체 15MiB 예산을 별도로 검사합니다. 게임의 비오디오 리소스 전체는 약 5.4MiB입니다.

저장 키/버전은 기존 v2를 유지합니다. Settings에 `masterVolume`, `musicVolume`, `sfxVolume`(0~1)을 추가하며, 이전 저장에는 기본 음량을 채우고 유효하지 않은 수치는 기본값 또는 범위 내 값으로 복구합니다. 음소거와 게임 기록은 보존합니다.

## 재생성·교체

```sh
python -m pip install numpy scipy imageio-ffmpeg
npm run audio:compose
npm run audio:audit
npm test
npm run build
npm run check:pages
```

정확한 편곡은 `scripts/compose-audio.py`에서 수정합니다. 파일을 다른 직접 제작 음원으로 교체할 때는 두 형식을 함께 갱신하고 매니페스트의 길이/루프를 수정한 다음 압축 후 검수를 반복하세요. 외부 API·토큰·로그인은 필요하지 않습니다.

## 검증 범위와 남은 제한

자동 검사: 전투 기존 29개와 오디오 13개, 총 42개 테스트. 자동재생 게이트, 비동기 음악 교체 경쟁, 곡 위치 재사용, 효과음 제한, 상태별 매핑, 문장 및 속성 효과 연결, 저장 기본값/복원, 대체 형식, 로드 실패를 검증합니다. 압축 파일 140개 전체를 다시 디코딩해 길이·피크·루프 경계·앞뒤 무음을 검사했습니다.

 실제 스피커/헤드폰의 물리 출력과 사람의 청취 평가는 이 환경에서 확인하지 못했습니다. Web Audio 최종 출력 계기의 비영(非零) 신호와 음소거 시 0, 오디오 그래프의 재생 상태를 기준으로 검증합니다. 실제 iOS/Android 기기는 별도 검증이 필요합니다. 합성 악기는 생악기 녹음의 세밀한 질감과 차이가 있습니다.


### Chromium 실제 브라우저 검증

- 새 포트의 별도 로컬 저장으로 검증했습니다. 사용자 기존 탐험은 덮어쓰지 않았습니다.
- 첫 입력 전 `locked`/음성 0. 음소거 해제 후 `running`, 타이틀 음악 1개와 최종 출력 RMS 약 0.03~0.05를 확인했습니다. 음소거 후 출력은 0입니다.
- 전체/음악/SFX 슬라이더를 Home/End/PageDown/방향키로 조절했습니다. 전체 80%, 음악 60%, 효과음 80%와 음소거 상태가 새로고침 후 정확히 복원됐습니다.
- 태일로 캐릭터 선택 → 도입 → 지도 → 일반 전투 → 개인 이벤트 → 그레이몬 → 강적 전투 → 메탈그레이몬 → 데블몬 → 승리를 진행했습니다. 최종 체력 53/100. 공장·이야기·강적·보스 음악의 서로 다른 ID와 출력 신호를 확인했습니다.
- 추가 탐험에서 실제 위험 선택으로 부담 7/진화 에너지 27을 쌓아 경고를 확인하고 스컬그레이몬으로 진화했습니다. 강제 진화 중 음악 정지와 스팅어 출력을 확인했고, 이후 방어하지 않고 턴을 넘겨 실제 HP 0 패배와 `battle-defeat` 재생을 확인했습니다.
- 8개 문장 징글은 같은 AudioManager를 사용하는 로컬 검증 페이지의 선택 UI로 각각 재생했습니다. 게임 발동 연결은 캐릭터 데이터·상태 변화 테스트에서 함께 검사합니다.
- 로컬 검증 페이지에서 존재하지 않는 OGG를 요청해 MP3 대체 재생을 확인했습니다. 두 형식 모두 없는 경우 실패 1건만 기록하고 기존 음악과 버튼 조작은 계속됐습니다. 이 의도적인 오류는 실제 게임 에셋 404와 구분합니다.
- 동일 효과음 30회 요청은 제한된 소스만 재생했습니다. 일반 플레이를 빠르게 진행해도 전체 음성은 최대 12개였습니다. 음악 재요청과 일시정지/복귀 후 음악 시작 횟수는 늘지 않았습니다.
- 실제 Web Audio suspend/resume 경로를 별도 검증 화면의 버튼으로 확인했습니다. 앱 내 탭 전환은 문서 visibility를 항상 바꾸지는 않아, OS/실제 모바일의 자동 백그라운드 전환은 추가 확인 대상입니다.
- 390×844 모바일 뷰포트에서 클릭 후 활성화, 수평 넘침 없음, 모든 설정 버튼/슬라이더 44px 이상을 확인했습니다. 데스크톱 화면과 키보드 조작도 확인했습니다.
- `/file-island-echoes/` 빌드의 212개 URL(오디오 140개 포함)이 정상 응답했습니다. 게임 탭에서 오디오 관련 치명적인 콘솔 오류는 없었습니다.

### GitHub Pages 공개 검증

2026-09-10, v0.5 빌드와 배포 성공. 공개 사이트의 212개 URL(오디오 140개 포함)을 확인했습니다. GitHub의 MP3 응답은 audio/mp3이며 로컬의 audio/mpeg와 함께 검사합니다. 공개 브라우저에서도 v0.5 표시와 첫 입력 전 오디오 잠금을 확인했습니다.
공개 사이트에서 소리 켜기 후 music-title 1개, 최종 출력 RMS 약 0.049, 콘솔 오류 없음도 확인했습니다. 검증 후 사용자의 기존 음소거 상태로 되돌렸습니다.


## v0.5.1 — 입력 효과음 지연 (2026-09-10)

확인된 주 원인은 처음 사용하는 큐의 fetch/decode 대기다. 기본 믹서의 캐시 재생 자체는 빠르지만 `await load()` 때문에 UI 전체 렌더 뒤로 밀릴 여지가 있었다. 정상 입력은 click에서 유지한다. 관측한 pointerup→click은 0.1~0.3ms로, pointerdown 확인음으로 바꿀 근거가 없었고 스크롤 오발음을 피했다. 비활성 버튼만 이동 10px 이내의 완료된 pointerup에서 거절음을 내며 후속 click을 명시적으로 중복 제거한다. Enter/Space/보조기기 click은 독립 처리한다.

`PRIORITY_SFX` 10개(ui-confirm/select/cancel/denied, card-select/attack/guard/support, node-select, energy-low)를 첫 활성화 직후 음악보다 먼저 다운로드·디코딩한다. 첫 입력 전 오디오 요청·컨텍스트는 여전히 0개다. 캐시 적중은 같은 이벤트 스택에서 source.start()를 호출해 UI 렌더를 기다리지 않는다. resume Promise를 공유하며 첫 활성화 확인음이 resume과 경합해 누락되지 않게 했다. 소리 켜기 버튼은 설정 적용 후 ui-confirm을 재생한다. 프리로드 실패는 실제 재생의 OGG/MP3 재시도를 막지 않는다. 기존 세 곡 음악 캐시, 12음성 제한, 징글 덕킹과 의도적 결과음 delay는 유지했다.

### 측정

Windows의 Codex in-app Chromium, 로컬 Vite 서버에서 실제 Web Audio를 계측했다. 네트워크 제한 도구 대신 오디오 fetch마다 200ms를 추가하는 개발용 제어 실험을 사용했다. 아래는 반복 통계가 아닌 관측 표본이며, 다른 입력이 끼어들지 않은 기록이다.

| 상황 | v0.5 기준 | v0.5.1 |
| --- | ---: | ---: |
| 독립 믹서 첫 활성화 + confirm | 263.5ms | 260.4ms |
| 처음 선택하는 ui-select | 217.8ms (cold) | 0.5ms (우선 준비 완료) |
| 이미 사용한 confirm | 0.1ms | 0.3ms |
| 실제 게임의 소리 켜기 | 기존 버튼에는 확인음 없음 | 240.7ms (cold) |
| 실제 게임 새 탐험/선택/지도/카드/턴 종료/설정 | — | 0~0.4ms (준비 완료) |

준비된 일반 효과음의 앱 내부 50ms 목표는 관측 표본에서 충족했다. 최초 활성화는 네트워크 영향을 여전히 받으며 약 260ms의 냉상태를 100ms 이내로 해결했다고 주장하지 않는다. 이후 최초 큐 사용은 준비된 캐시로 처리한다. 기본 AudioContext와 `{latencyHint:'interactive'}` 모두 baseLatency=10ms, outputLatency=0을 보고해 생성 옵션은 변경하지 않았다. outputLatency=0은 실제 하드웨어 지연이 없다는 뜻이 아니다. 실제 iOS/Android와 Bluetooth 출력 청감 100ms 목표는 미검증이다.

효과음의 예약 추가 지연은 0ms, 카드 결과음은 기존 60~500ms 등 의도적 간격을 유지했다. 냉상태의 비우선 결과음에는 다운로드 시간도 더해질 수 있다. 우선 10개 파일의 -60dB 첫 신호는 OGG 0~1.219ms, MP3 0~0.969ms로 0.5초 무음은 없었다. 자세한 코덱별 값은 `docs/audio-measurements.json`에 기록했다.

### 예산과 재현

- 우선 10개: OGG 58,489bytes(57.1KiB), MP3 37,304bytes(36.4KiB). 정상적으로 한 형식만 받는다.
- PCM 예상: 48kHz stereo float32 기준 약 1,420,800bytes(1.36MiB). 실제 리샘플링/코덱 길이에 따라 조금 달라진다.
- 전체 오디오: 14,490,553bytes(13.82MiB), 파일 추가·음악 변경 없음.
- `?audioDebug&audioDelay=200`은 개발 모드 전용이며 배포 JS에서 제거된다. 입력/로드/디코딩/start 시각은 로컬 DOM에만 표시한다. 재현 절차는 `qa/README.md` 참고.

## v0.9.0 터치 선행 다운로드

사용자 추가 요청으로 첫 입력 전 터치 피드백 7종의 지원 코덱 압축 데이터를 준비한다. AudioContext나 재생은 시작하지 않는다. PWA shell에는 OGG/MP3 14파일 63,235B를 포함하며 음악은 제외한다. 기존 unlock 뒤 우선 효과음 디코딩과 메모리 버퍼 재생 경로를 유지한다. 로컬 200ms fetch 지연 비교에서 새 mixer의 최초 ui-select source.start는 347ms, 압축 데이터 준비 후 새 mixer는 77.6ms였다. 이 수치는 브라우저 예약 지연이며 실제 Android/iOS 스피커 지연을 나타내지 않는다. `qa/touch-preload.html`로 재현한다.
