# 실행 검증 기록

검증일: 2026-09-09. 환경: Windows, Node.js 24.14.1, Codex 내장 Chromium 브라우저.

## 자동 검증

`npm test`: **21개 통과**.

- 시작 덱/드로우/에너지, 경로 제한, 카드 비용과 버림 처리
- 방어로 피해 차단 후 다음 턴 초기화, 버림 더미 재섞기, 드로우 카드 자기 재추첨 방지
- 적 7종의 서로 다른 패턴, 약화, 오염, 적 방어
- 두 적 대상 선택, 전체 공격, 사망한 대상 자동 전환
- 일반 패배와 자기 손상에 의한 패배
- 보상 3종 중 선택/건너뛰기, 중복 완료 방지
- 모든 이벤트의 두 선택지 효과, 진화 조건·전용 카드·공격/방어 보너스와 스컬그레이몬 반동
- **시드 1, 7, 42, 99, 2026 각각에서 메탈그레이몬 및 스컬그레이몬 경로 완주**: 10회 모두 11개 장소 완료, 전투 5승, 데블몬 처치
- 전투 저장 완전 왕복, 손상된 JSON·버전·스키마·중첩 참조, 저장 API 거부/용량 초과/접근자 SecurityError
- 도감 결과 중복 집계 방지, 다음 탐험 능력치 초기화

`npm run build`: TypeScript 검사와 Vite 정적 빌드 성공. `npm install` 최종 의존성 감사: 알려진 취약점 0개. 플레이용 외부 요청이나 AI API는 없습니다.

첫 버전 검증 당시 `dist/`: 15개 파일, 원본 합계 **277,996바이트**, 파일별 gzip 합계 **39,149바이트**. SVG 12개를 모두 포함한 수치입니다. `npm run preview`로 실제 빌드의 타이틀·새 탐험·지도, 이미지 로딩과 가로 넘침 없음도 확인했습니다. 이후 배포 준비 변경은 아래 추가 검증 기록을 따릅니다.

## 브라우저에서 직접 확인

테스트 코드를 이용해 체력이나 덱을 주입하지 않고, 화면에 있는 버튼의 클릭 및 키보드 Enter로 진행했습니다.

| 항목 | 확인 결과 |
| --- | --- |
| 타이틀, 새 탐험, 첫 안내, 지도 | 정상 |
| 카드 사용, 추가 에너지/드로우, 방어, 턴 종료 | 정상 |
| 보상 카드 선택과 건너뛰기 | 덱과 다음 노드에 반영 |
| 한솔/정석/매튜/리키/나리 이벤트, 소라 휴식, 위험 이벤트 | 대화와 선택 효과 확인 |
| 아구몬 → 그레이몬 → 메탈그레이몬 | 미리보기, 연출, 카드 변화, 데블몬 처치·결과 화면 확인 |
| 아구몬 → 그레이몬 → 스컬그레이몬 | 경고와 명시적 선택, 자기 손상·전용 카드, 데블몬 처치·결과 화면 확인 |
| 패배 | 두 에렉몬 전투에서 턴을 넘겨 HP 0, 패배 결과 확인 |
| 도감 | 진화체·적·이벤트·구역 발견과 클리어 수 반영 |
| 진화 직후 새로고침 | 동일한 진화체·덱·체력으로 이어하기 |
| 보스전 중 새로고침 | HP 86/90, 데블몬 HP 8/148, 3턴, 손패 3장이 **완전히 일치** |
| 두 적 대상 선택 | 두 번째 에렉몬만 28 → 21, 첫 번째는 28 유지 |
| 390px 세로 모바일 | 카드 효과 상시 표시, 가로 손패 탐색, 적 선택·카드 사용·턴 종료 가능, 페이지 가로 넘침 없음 |
| 1280px 데스크톱 | 지도, 타이틀, 진화 경고, 전투 레이아웃 확인 |
| 설정 | 효과음, 모션 감소, 안내 생략 변경 후 새로고침에도 유지 |
| 키보드 | Enter 조작과 카드 사용 후 포커스 유지 확인 |
| 아트 파일 | 화면 이미지 로딩 확인, 원본 SVG 12개 제공 |

## 검증 범위의 한계

- 실제 iPhone/Android 기기, Safari/Firefox, 스크린리더의 전체 탐험은 미검증입니다.
- 오디오 설정과 Web Audio 실행 경로는 확인했지만 실제 스피커 출력의 청음 검증은 하지 않았습니다.
- 손상된 저장과 브라우저 저장 차단은 자동 테스트로 확인했으며 브라우저 정책을 실제로 변경하지 않았습니다.
- 미나를 포함한 모든 이벤트 선택 효과는 자동 검증했지만 모든 갈림길 조합을 브라우저에서 완주한 것은 아닙니다.
- 실제 사용자의 10–15분 소요 시간과 반복 플레이 밸런스는 추가 플레이테스트가 필요합니다.
- GitHub Pages 원격 배포는 하지 않았습니다. 프로젝트 하위 경로용 정적 빌드만 준비되었습니다.

## GitHub Pages 및 제한적인 공유 준비 · 추가 검증

- `base: '/file-island-echoes/'`로 변경 후 `npm run build` 성공, 기존 게임 테스트 21개 통과.
- `npm run check:pages` 통과: 빌드 16개 파일의 프로젝트 경로, noindex 지시, robots.txt, 홍보 메타데이터 부재, 공통 토큰/개인 경로 패턴과 배포 제외 파일 검사.
- 소스·문서에서도 실제 개인 파일 경로와 공통 자격 증명 패턴을 점검했습니다. 검사 스크립트 안의 탐지용 정규식만 검색에 잡혔으며 실제 비밀 값은 발견하지 않았습니다. 일반 패턴 검사가 모든 형태의 비밀정보를 탐지한다고 보장하지는 않습니다.
- Pages 워크플로 YAML 파싱 성공. main 브랜치 제한과 deploy의 build 의존성 확인. 테스트·빌드·경로 검사 이후에만 `dist/` 업로드, 성공한 build 이후에만 배포하도록 구성.
- 로컬 프로덕션 미리보기의 `/file-island-echoes/`, `/file-island-echoes/index.html`, robots.txt, SVG 12개: 총 15개 URL의 HTTP 200 확인. SVG 응답 형식도 확인.
- 브라우저에서 JS/CSS 및 이미지 주소가 `/file-island-echoes/` 아래이고 이미지가 정상 로드되는 것을 확인.
- 하위 경로에서 첫 전투 진입·공격 후 새로고침 및 이어하기: 플레이어 HP 64/64, 적 HP 27/34, 에너지 2, 손패 4장이 동일하게 복원됨.
- `index.html` 직접 URL로 접속해 타이틀과 이어하기 정상 실행, 콘솔 오류 없음.
- 게임 상단 **정보**에서 영문·한국어 비공식/비영리 고지와 권리자 비제휴 문구 확인. 390px 모바일에서 고지 모달 너비 343px, 페이지 가로 넘침 없음.
- 이 배포 준비 검증 시점에는 원격 생성·push·설정 변경을 수행하지 않았습니다. 이후 사용자의 저장소 업로드 요청에 따라 기존 원격 초기 커밋을 보존하는 방식으로 main 업로드를 준비했습니다. Pages 공개는 여전히 별도 요청 대상입니다.
- robots.txt의 도메인 루트 위치 제약, 크롤링 차단 시 noindex를 읽지 못하는 제약과 접근 통제가 아니라는 사실을 README에 명시했습니다.

## 저장소 업로드와 사이트 공개 분리

- main push는 게임 테스트·빌드·Pages 파일 검사를 실행합니다.
- `PAGES_ENABLED=true`라는 저장소 Actions 변수가 없으면 Pages 구성·아티팩트 업로드·배포는 건너뜁니다. 코드 업로드만 요청한 상황에서 사이트를 공개하지 않도록 분리했습니다.
- 원격 초기 `.gitignore`의 규칙과 커밋 이력을 보존합니다. 커밋 작성자에는 GitHub noreply 이메일을 사용합니다.
