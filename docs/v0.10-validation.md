# v0.10.0 검증 — 2026-09-15

선행 v0.9.1 `55cb002`, Pages Actions `34914030198` 성공 후 구현을 시작했다.

- 자동 테스트 146개 통과. 8명 고유 seed/전투/덱/플래그·저장 시각 전체 비교, v1/v2 이관 재시도, 빈 v3 우선순위, 손상 슬롯·미지 키·중복/위험 키, quota rollback, 탭 충돌, 결과 중복 방지, format 1/2 및 긴 로그 round-trip 포함.
- `npm run build`, `npm run check:pages` 성공. 로컬 프로덕션 HTTP 223 URL 검사 통과.
- 전투 기준 비교 608 시나리오·8명 진화 일치. 엔진 규칙 파일은 변경하지 않음.
- 0.9.1 서비스워커에서 0.10.0 적용 후 신태일 보상 체크포인트가 슬롯으로 이관되는 것을 확인.
- 실제 제품 UI로 리키 시작→전투에서 희망 카드 사용→홈→신나리 새 탐험→전투→리키→신나리 왕복. 리키 손패 4장 유지. 진단 UI에서 모든 슬롯 Run과 updatedAt 원문이 동일함을 확인.
- 같은 신나리 출발 시 교체 모달이 현재 이름·장소와 다른 7명 유지 안내를 표시. 취소 뒤 3슬롯 원문 동일. 다른 아이 출발에는 교체 모달 없음.
- 별도 로컬 탭에서 8개 합성 슬롯을 설치하자 게임 탭에 외부 변경 경고와 최신 기록 불러오기 표시. 오래된 탭의 조용한 덮어쓰기 방지.
- 실제 브라우저 iframe에서 320×740, 390×844, 412×915, 1280×900 및 320px/200% 글자 확대 검증. 슬롯 버튼 scrollWidth=clientWidth, 높이 108~347px, dialog가 화면 안에 유지. 모바일 스크린샷에서 한글 줄바꿈·닫기·이어하기 식별 확인.
- 8슬롯 중 신나리만 귀환해 결과 상태로 만든 뒤 대기 중인 최종 PWA 빌드를 적용했다. 적용 전후 8개 Run/updatedAt 원문이 모두 동일했고 대표 버튼은 신나리 결과 보기로 표시됐다.
- 오프라인: 서버 중단 후 재실행, 8개 슬롯 목록 복구 및 모든 Run/updatedAt 원문 동일. v0.9.1 음악 준비됨 유지.
- JSON 3슬롯 브라우저 round-trip(9,483B) 성공. 8슬롯 긴 로그 자동 검증 성공. 실제 문구 최대 로그 용량 계산은 `scripts/measure-slot-save.mjs`: 1,315,123B. 키 순서는 비교에서 제외하고 값은 정확히 비교한다.
- 쉘몬과 데블몬 대상 링 스크린샷 회귀 검토. 발 아래 타원·곡선 오라가 유지된다.

실물 Android/iOS standalone 설치·터치 하드웨어는 미검증이다. 412×915는 Galaxy S23+에 대응하는 CSS 크기 검사이며 기기 인증이 아니다. quota/읽기 실패는 자동 테스트, 교차 탭 경고와 PWA 업데이트/오프라인은 Chromium에서 확인했다. localStorage의 동시 compare-and-swap 한계는 SAVE.md에 명시했다.

새 에셋이나 라이브러리는 추가하지 않았다. 필수 PWA 팩 88파일 15,899,841B, 음악 24파일 13,266,249B. 전체 정적 파일 22,876,923B; 큰 일러스트는 지연 로딩. 실행: `npm ci`, `npm run dev`; 배포 빌드: `npm run build`.

배포: 구현 커밋 `8c910a4`, Pages Actions `34915548687` 성공. 공개 주소의 현재 빌드 및 223개 URL HTTP 검사 통과. 기존 공개 PWA에 진행 중 전투가 있으면 새 버전이 대기하고 전투 종료 후 적용하는 기존 안전 정책도 확인했다.
