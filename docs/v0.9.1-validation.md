# v0.9.1 검증 — 2026-09-15

- `npm test`: 132개 통과. 알파 접지 범위, 이야기 실제 증감, 음악 전체/변경분 설치·실패·quota·오프라인 회귀 포함.
- `npm run build`, `npm run check:pages`: 성공. 필수 팩 88파일, 15,894,722B; 음악 24파일 13,266,249B.
- `node scripts/verify-combat-baseline.mjs`: 608개 전투 시나리오와 8명 진화 판정 일치(로그 표현 제외).
- 로컬 브라우저: 쉘몬 전투 접지 좌표, 모바일 이야기 선택에서 숫자 사전 표시 없음, 선택 후 유대 +2/진화 에너지 +2 표시 확인.
- 실제 0.9.0 서비스워커에서 0.9.1 업데이트 적용, 기존 탐험 유지, 설정의 오프라인 음악 준비됨 확인.
- 로컬 서버 중단 후 앱 새로고침 성공. 진단 UI에서 캐시의 24개 OGG/MP3 모두 순차 디코딩 성공(각 64~76초). 전곡을 동시에 메모리에 보관하지 않음.
- 실물 Android/iOS 설치·스피커 출력은 미검증. CSS viewport는 실기기 인증이 아니다.

구현 파일: `src/ground.ts`, `src/ground-metrics.json`, `src/event-result.ts`, `pwa/worker.mjs`, `src/pwa.ts`. `scripts/ground-metrics.py`는 아트 교체 후 접지 메트릭 재생성용이다. 게임 규칙·저장 스키마는 변경하지 않았다.
