# 실행 검증 기록 · 2026-09-10 · v0.3

## 자동 검증

- `npm test`: 28개 통과. 전투 기본 규칙, 무료 에너지 제거, 회복 제한, 예측 피해, 보스 단계 강화/흡수, 두 진화 경로, 보상, 저장 복원·손상·차단과 도감 갱신을 검사합니다.
- `npm run balance`: 고정 시드 800회, 두 경로 × 두 진화체 × 두 전략. 전부 유한한 턴 안에 승패 확정. 표와 재현 방법은 [BALANCE.md](BALANCE.md)에 있습니다. 자동 전략의 결과이며 사람의 승률을 뜻하지 않습니다.
- `npm run build`: TypeScript와 Vite 프로덕션 빌드 성공.
- `npm run check:pages`: 58개 파일, 23개 이미지, 프로젝트 base, noindex/robots, 일반적인 비밀정보·개인 경로 패턴, base64 래스터 부재와 용량 검사 통과.
- `node scripts/check-http.mjs`: 로컬 프로덕션의 기본 URL, index.html, robots.txt, 엔트리 JS/CSS, 파비콘과 WebP 23장, 총 29개 URL HTTP 200. 현재 빌드 해시와 WebP Content-Type도 확인합니다.

## 실제 브라우저

Chromium 브라우저의 실제 버튼·카드·키보드 조작으로 진행했습니다. 저장 데이터나 숨겨진 게임 함수를 주입하지 않았습니다.

| 항목 | 확인 내용 |
| --- | --- |
| 타이틀 | 풍경·태일/아구몬 삽화와 새 탐험/이어서 탐험/안내 메뉴 |
| 지도 | 풍경 위 갈림길, 지나온 길, 현재 위치. 다른 구역을 미리 봐도 아직 갈 수 없는 노드는 비활성화 |
| 일반 경로 | 아구몬 → 그레이몬 → 메탈그레이몬 → 데블몬 승리. 결과 11개 장소·5전투·HP 51/100 |
| 강제 진화 | 위험 설명과 직접 확인 → 스컬그레이몬 → 데블몬전 패배. 결과 10개 장소·4전투·HP 0/90 |
| 일반/강제 경로 승리 가능성 | 두 경로 모두 고정 시드 자동 검증에서 승리 확인. 이번 브라우저 강제 경로는 패배했으며 승리했다고 주장하지 않음 |
| 지원/휴식/보상 | 한솔·정석·소라·매튜·리키·나리·태일 이벤트, 두 번째 경로의 위험 선택, 카드 보상/건너뛰기 확인 |
| 진화 카드 | 메가 플레임, 피해 25 기가 디스트로이어, 그라운드 제로·자기 손상. 카드 면에 진화 보너스가 포함된 실제 수치 표시 |
| 적 두 마리 | 320px 화면에서 두 번째 에렉몬 클릭 선택. 방어 카드 클릭 후 에너지 3 → 2, 예상 피해 14 → 7 |
| 보스 | 시작 공격 20, 6턴에 폭주 +6 예고. 보스 강화 일정과 최종 행동 수치 표시 |
| 새로고침 | index.html 직접 접근 후 이어하기. 공장 전투 HP 80/80, 적 HP 96, 손패와 대상/행동이 동일하게 복원 |
| 도감 | 결과 갱신, 진화체 4종·적 7종 이미지 누락 없음. 과거 기록 유지 |
| 움직임 줄이기 | 설정 유지. 강제 진화에서 이전 실루엣·플래시 display:none, 새 이미지 animation:none·opacity:1 |
| 1280×900 | 타이틀·지도·전투·보상·진화·보스·결과 화면 확인. 전장과 손패를 한 화면에서 읽을 수 있게 높이 축소 |
| 390×844 / 320×740 | 이벤트·지도·일반/강제 진화·단일/두 적 전투 확인. 문서 가로 넘침 없음. 핵심 버튼 44px 이상, 손패는 내부 가로 스크롤 |
| 오류 | 확인한 플레이 흐름에서 콘솔 오류와 깨진 이미지 없음 |

표준 경로: 0a → 1a → 2b → 3a → 4b → 5a → 6a → 7a → 8a → 9a → 10a.
강제 경로는 5b·6b에서 위험 선택. 별도로 1b → 2a에서 보급 이벤트와 적 둘을 확인했습니다.

## 배포와 용량

- Pages는 기존 main 전용 Actions 배포를 유지합니다. 테스트·빌드·경로 검사 성공 후 동일 사이트에 반영합니다.
- 사이트: https://jimyeong-han.github.io/file-island-echoes/
- 외부 점검 명령: `node scripts/check-http.mjs https://jimyeong-han.github.io/file-island-echoes/`
- 새 이미지는 추가하지 않았습니다. 기존 새로 제작한 팬 일러스트 23장과 SVG fallback을 유지합니다.
- WebP·UI 에셋 약 2.19 MiB. 전체 배포의 gzip 추정 전송량 약 2.23 MiB, 초기 타이틀 약 0.36 MiB. 타이틀 이미지는 숲 배경과 태일/아구몬 초상화 2장입니다.
- CSS는 이전 약 60.8 kB에서 약 34.0 kB로 줄었습니다. 서버 압축·캐시에 따라 실제 요청량은 다릅니다.
- noindex와 robots.txt는 접근 통제가 아닙니다. 공개 URL과 공개 저장소는 누구나 접근할 수 있습니다.

## 검증 한계

- 모바일은 Chromium 뷰포트 검사입니다. 실제 iOS/Android, Safari/Firefox, 스크린리더 전체 플레이는 확인하지 않았습니다.
- 사람 대상 승률·소요 시간 조사는 하지 않았습니다. 스컬그레이몬은 자해 부담 때문에 경로와 덱에 따라 특히 어렵습니다.
- 실제 스피커 청음, 브라우저 네트워크 장애 주입은 하지 않았습니다. 기존 합성 효과음·음소거·이미지 fallback은 유지합니다.
- 기존 진행 중 전투의 적 HP는 저장값을 유지하고 카드·패턴은 새 규칙을 사용합니다. 전체 밸런스 확인은 새 탐험이 정확합니다.
